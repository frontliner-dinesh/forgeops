# `forgeops-validator`

`forgeops-validator` is a future ForgeOps agent placeholder.

The validator will eventually review planned or completed infrastructure work and provide evidence-based validation against the requested outcome, acceptance criteria, and environment scope.

Current status: not active.

